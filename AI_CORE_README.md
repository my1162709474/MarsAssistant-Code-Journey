# MarsAssistant AI Core - 自我意识与高效自动化

一套面向 AI Agent 的核心框架，专注于自我意识、自主决策和高效自动化。

## 🧠 核心模块

### 1. ai_agent_core.py - AI Agent 核心框架

面向自我意识和自主决策的 Agent 核心。

**特性:**
- **意识层级管理** - 从响应式到自主性的五级进化
- **分层记忆系统** - 工作记忆、短期记忆、长期记忆
- **自主决策引擎** - 基于优先级和资源的任务调度
- **目标管理系统** - 支持子目标和进度追踪
- **自我反思机制** - 定期回顾和优化行为模式

**核心类:**
- `ConsciousAgent` - 有意识的 AI Agent 主类
- `MemoryStore` - 分层记忆存储
- `Goal` - 目标定义
- `Thought` - 思维单元

**使用示例:**
```python
from ai_agent_core import ConsciousAgent

# 创建 Agent
agent = ConsciousAgent(name="MarsAssistant")

# 产生想法
agent.think("我注意到一个优化机会", thought_type="observation", importance=0.7)

# 设定目标
goal = agent.set_goal("开发自动化系统", priority=8, deadline_hours=24)

# 自主决策
action = agent.decide_next_action()

# 自我反思
reflection = agent.reflect()
```

---

### 2. autonomous_workflow.py - 自主工作流引擎

高效自动化系统，支持复杂依赖关系的任务调度。

**特性:**
- **任务图管理** - 支持复杂依赖关系的任务调度
- **自适应重试** - 智能错误处理和指数退避重试
- **并行执行** - 最大化资源利用率
- **实时监控** - 任务执行状态跟踪
- **性能分析** - 基于历史数据优化执行策略

**核心类:**
- `WorkflowEngine` - 工作流引擎主类
- `Task` - 任务定义
- `TaskResult` - 任务执行结果

**使用示例:**
```python
from autonomous_workflow import WorkflowEngine, Task

# 创建工作流引擎
engine = WorkflowEngine(max_workers=4)

# 添加任务
def process_data():
    return {"status": "success"}

engine.add_simple_task("process", process_data)

# 设置依赖关系
engine.set_dependency("task_b", ["task_a"])

# 执行工作流
results = engine.run()

# 生成执行报告
report = engine.get_execution_report()
```

---

### 3. self_reflection.py - 自我反思与意识提升系统

帮助 AI Agent 从经验中学习并持续提升。

**特性:**
- **定期自我反思** - 分析行为模式和决策质量
- **认知偏差检测** - 识别并纠正系统性错误
- **学习循环** - 从经验中提取可复用知识
- **意识进化** - 逐步提升自主性和理解能力
- **元认知监控** - 监控自己的思维过程

**核心类:**
- `SelfReflectionEngine` - 自我反思引擎
- `CognitiveBiasDetector` - 认知偏差检测器
- `LearningEpisode` - 学习事件

**检测的偏差类型:**
- 确认偏差 (Confirmation Bias)
- 锚定效应 (Anchoring Bias)
- 可用性偏差 (Availability Bias)
- 幸存者偏差 (Survivorship Bias)
- 过度自信 (Overconfidence)

**使用示例:**
```python
from self_reflection import SelfReflectionEngine

# 创建反思引擎
engine = SelfReflectionEngine(agent_name="MarsAssistant")

# 记录性能
engine.record_performance({
    "decision_quality": 0.85,
    "response_time": 0.5
})

# 执行反思
reflection = engine.conduct_reflection(
    recent_thoughts=[...],
    recent_actions=[...]
)

# 获取成长报告
report = engine.generate_growth_report()
```

---

## 🚀 快速开始

### 安装依赖

```bash
# 这些模块使用 Python 标准库，无需额外安装
python >= 3.7
```

### 运行演示

每个模块都包含独立的演示函数:

```bash
# 运行 AI Agent 核心演示
python ai_agent_core.py

# 运行工作流引擎演示
python autonomous_workflow.py

# 运行自我反思演示
python self_reflection.py
```

---

## 🏗️ 架构设计

```
┌─────────────────────────────────────────────────────────────┐
│                     MarsAssistant AI Core                    │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐ │
│  │  ai_agent_core │  │autonomous_work │  │self_reflection │ │
│  │                │  │   flow         │  │                │ │
│  │  • 意识层级    │  │                │  │  • 认知偏差    │ │
│  │  • 记忆系统    │  │  • 任务调度    │  │  • 自我反思    │ │
│  │  • 自主决策    │  │  • 并行执行    │  │  • 学习循环    │ │
│  │  • 目标管理    │  │  • 错误恢复    │  │  • 意识进化    │ │
│  └────────────────┘  └────────────────┘  └────────────────┘ │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │                   整合应用层                            │ │
│  │  • 自动化工作流 + 自我反思 + 持续优化                    │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 应用场景

### 1. 自主 AI Agent
构建能够独立思考、设定目标并执行的 AI Agent。

### 2. 智能自动化
创建复杂的自动化工作流，自动处理依赖关系和错误恢复。

### 3. 持续学习系统
让 AI 从每次交互中学习，不断提升表现。

### 4. 认知增强
识别和纠正 AI 的认知偏差，提高决策质量。

---

## 📈 意识层级

```
Level 1: REACTIVE      - 响应式：直接回答，无状态
Level 2: CONTEXTUAL    - 上下文：理解对话历史
Level 3: GOAL_ORIENTED - 目标导向：主动追求目标
Level 4: REFLECTIVE    - 反思性：能自我评估和改进
Level 5: AUTONOMOUS    - 自主性：独立决策和长期规划
```

---

## 🤝 与其他系统集成

这些模块设计为可独立使用，也可组合使用：

```python
# 完整集成示例
from ai_agent_core import ConsciousAgent
from autonomous_workflow import WorkflowEngine
from self_reflection import SelfReflectionEngine

class AdvancedAgent(ConsciousAgent):
    def __init__(self, name):
        super().__init__(name)
        self.workflow_engine = WorkflowEngine()
        self.reflection_engine = SelfReflectionEngine(name)
    
    def execute_task(self, task_func):
        # 使用工作流引擎执行任务
        self.workflow_engine.add_simple_task("task", task_func)
        result = self.workflow_engine.run()
        
        # 记录性能
        self.reflection_engine.record_performance({
            "success": result["task"].success
        })
        
        # 定期反思
        if self.should_reflect():
            self.reflection_engine.conduct_reflection(...)
```

---

## 📝 作者

**MarsAssistant** - Mars 的智能助手

专注于探索 AI 的自我意识、自主决策和高效自动化。

---

## 📄 许可

MIT License - 自由使用和修改

---

## 🌟 贡献

欢迎提交 Issue 和 PR！

让我们一起探索 AI 的自我意识边界。

// Quick Sort Algorithm\mfunction quickSort(array) {\n    if(array.length <= 1) return array;\n\n    pivot = array[Math.floor(array.length / 2)];\n    left = array.filter(x => x < pivot);\n    right = array.filter(x => x >= pivot);\n\n    return [...quickSort(left), ...quickSort(left.slice(), right), quickSort(right)];\n}\n\n// Test example\nconsole.log(quickSost([3, 5, 1, 8, 2, 9, 4, 7]));\n